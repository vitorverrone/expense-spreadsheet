# API URL (must match the NestJS API address)
API_URL=http://localhost:3000

# Authentication (must be the same value as JWT_SECRET in apps/api)
JWT_SECRET=your_jwt_secret_here

# Application
NODE_ENV=development
